# 🚰 Calculate_Water_Bills (โปรแกรมคำนวณค่าน้ำประปา)

[![Project Status](https://img.shields.io/badge/Status-Completed-brightgreen.svg)](https://github.com/james28dev/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](https://github.com/James28Dev/Calculate-Water-Bills/blob/main/LICENSE)
[![Language](https://img.shields.io/badge/Language-C-blue.svg)](https://en.wikipedia.org/wiki/C_(programming_language))

> โปรเจกต์รายวิชา **Computer Programming** ที่ **Songkhla Rajabhat University**
> โปรแกรมคำนวณค่าบริการน้ำประปาของการประปาส่วนภูมิภาค (Provincial Waterworks Authority - PWA) โดยพิจารณาจากพื้นที่ ประเภทผู้ใช้ และขนาดมาตรวัดน้ำ รวมถึงการคำนวณภาษีมูลค่าเพิ่ม (VAT 7%)

---

## ✨ คุณสมบัติหลัก (Features)

* **คำนวณอัตราก้าวหน้า (Tiered Rate Calculation):** สามารถคำนวณค่าน้ำตามอัตราขั้นบันไดการใช้น้ำของ PWA ได้อย่างแม่นยำ
* **รองรับหลายประเภท:** รองรับการคำนวณสำหรับ 3 พื้นที่หลัก และ 4 ประเภทผู้ใช้งาน
* **ค่าบริการมาตรวัดน้ำ (Service Fee):** คำนวณค่าบริการคงที่ตามขนาดมาตรวัดน้ำ
* **แสดงตารางอัตรา (Tariff Graph):** มีฟังก์ชัน `outputGraph` สำหรับแสดงตารางอัตราค่าบริการ (Price Tariff) สำหรับการใช้น้ำในแต่ละประเภท
* **โครงสร้างภาษา C:** ใช้ **Struct** (`provincialWaterworksAuthority`) ในการจัดเก็บข้อมูลผู้ใช้และใช้ **Pointer** ในการส่งผ่านข้อมูล (Pass by Reference) ระหว่างฟังก์ชันอย่างมีประสิทธิภาพ

---

## 🛠️ เทคโนโลยีที่ใช้ (Built With)

* **ภาษาโปรแกรม:** C (ใช้มาตรฐาน `stdio.h` และ `string.h`)
* **สภาพแวดล้อม (Environment):** คอมไพล์และรันบนสภาพแวดล้อม **Console (Command Line)**

---

## 🚀 การเริ่มต้นใช้งาน (Getting Started)

### Prerequisites

คุณจำเป็นต้องมี **C Compiler** ที่ติดตั้งบนระบบของคุณ (เช่น GCC หรือ MinGW บน Windows/Linux)

* สำหรับผู้ใช้ **Fedora Linux**:
    ```bash
    sudo dnf install gcc
    ```
* สำหรับผู้ใช้ **Windows**:
    * ติดตั้ง **MinGW** หรือใช้ **WSL (Windows Subsystem for Linux)** เพื่อเข้าถึง GCC

### Installation และ Running

1.  **Clone Repository:**
    ```bash
    git clone https://github.com/James28Dev/Calculate-Water-Bills.git
    cd Calculate_Water_Bills
    ```

2.  **Compile Source Code:** ใช้ GCC ในการคอมไพล์ไฟล์ `.c` ให้เป็นไฟล์ปฏิบัติการ (Executable File)
    ```bash
    gcc "Project - Calculate Water Bills.c" -o water_calc
    ```

3.  **Run Program:**
    ```bash
    ./water_calc  # สำหรับ Linux/Fedora
    # หรือ
    .\water_calc.exe # สำหรับ Windows
    ```

---

## 📂 โครงสร้างโค้ดที่สำคัญ (Core Code Structure)

โค้ดใช้การจัดระเบียบในรูปแบบฟังก์ชันที่ชัดเจน โดยมีโครงสร้างข้อมูลหลักคือ **Structure** ดังนี้:

| Structure/Function | บทบาท |
| :--- | :--- |
| `dataWater` | Global Structure ที่เก็บข้อมูลทั้งหมดของผู้ใช้ (พื้นที่, ประเภท, ขนาดมาตร, ปริมาณการใช้) |
| `calculate` | ฟังก์ชันหลักในการคำนวณค่าใช้จ่าย **น้ำ** ตามอัตราก้าวหน้า และรวม VAT 7% |
| `serv` | ฟังก์ชันสำหรับกำหนด **ค่าบริการรายเดือน** ตามขนาดมาตรวัดน้ำ |
| `outputGraph` | ฟังก์ชันสำหรับแสดงตารางอัตราค่าบริการของ PWA (Tariff Table) |

---

## ⚖️ ใบอนุญาต (License)

โปรเจกต์นี้เผยแพร่ภายใต้ **MIT License** ดูรายละเอียดเพิ่มเติมในไฟล์ [LICENSE](https://github.com/James28Dev/Calculate-Water-Bills/blob/main/LICENSE)
